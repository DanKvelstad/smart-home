version: '3'
services:
  vpn:
    build: vpn
    volumes:
      - secrets:/secrets
    cap_add:
      - NET_ADMIN
    devices:
      - "/dev/net/tun"
  ikea-tradfri-gateway:
    build: ikea-tradfri-gateway
    depends_on: 
      - vpn
volumes:
  secrets:
    external: true